# Project ODIN Models

Place optional 3D model assets here. The loader manifest is `src/game/modelManifest.js`.

Folders:

- `buildings/`
- `props/`
- `nature/`
- `landmarks/`

The site keeps procedural fallback geometry, so missing model assets will not break the world. Use optimized web-ready assets from compatible libraries or external generation tools.
